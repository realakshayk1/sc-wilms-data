# Pip-installable subset (see environment.yml for full conda env)
scanpy>=1.10
anndata>=0.10
scikit-learn>=1.4
scikit-image>=0.22
scipy>=1.12
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
pytest>=8.0
opencv-python-headless>=4.9
pyarrow>=15.0
tifffile>=2024.2
imageio>=2.34

# Phase B — foundation-model embeddings (Phikon) + attention-MIL
torch>=2.2
torchvision>=0.17
transformers>=4.40

# Phase B — StarDist nuclei segmentation (needs a TensorFlow backend)
tensorflow-cpu>=2.15
stardist>=0.9
csbdeep>=0.8
# cellpose>=3.0  # optional segmentation fallback
